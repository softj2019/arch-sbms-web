<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.archivsoft.sbms.mapper.SettingMapper">
    <!-- 기본 설정값 조회 -->
    <select id="getSetting" parameterType="SettingDTO" resultType="SettingDTO">
        SELECT  id
              , description
              , option_1 AS setting
              , value_1 AS value
              , 'option1' AS optionKey
        FROM    tb_general_setting
        WHERE   id = 1
        UNION ALL
        SELECT  id
              , description
              , option_2 AS setting
              , value_2 AS value
              , 'option2' AS optionKey
        FROM    tb_general_setting
        WHERE   id = 1
        UNION ALL
        SELECT  id
              , description
              , option_1 AS setting
              , value_1 AS value
              , 'option1' AS optionKey
        FROM    tb_general_setting
        WHERE   id = 2
        UNION ALL
        SELECT  id
              , description
              , option_1 AS setting
              , value_1 AS value
              , 'option1' AS optionKey
        FROM    tb_general_setting
        WHERE   id = 3
        UNION ALL
        SELECT  id
              , description
              , option_2 AS setting
              , value_2 AS value
              , 'option2' AS optionKey
        FROM    tb_general_setting
        WHERE   id = 3
        UNION ALL
        SELECT  id
              , description
              , option_1 AS setting
              , value_1 AS value
              , 'option1' AS optionKey
        FROM    tb_general_setting
        WHERE   id = 4
    </select>

    <update id="updateSetting" parameterType="com.archivsoft.sbms.dto.SettingDTO">
        UPDATE tb_general_setting
        <set>
            <trim suffixOverrides=",">  <if test="setting.value1 != null and setting.value1 != ''">
                value_1 = #{setting.value1},
            </if>
                <if test="setting.value2 != null and setting.value2 != ''">
                    value_2 = #{setting.value2},
                </if>
                <if test="setting.value3 != null and setting.value3 != ''">
                    value_3 = #{setting.value3},
                </if>
            </trim>
        </set>
        WHERE id = #{setting.id}
    </update>

    <!-- 접근가능 ip 조회 -->
    <select id="getIp" resultType="String">
        SELECT  value_1
        FROM    tb_general_setting
        WHERE   id = 4
    </select>
</mapper>