<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.archivsoft.sbms.mapper.CommandMapper">

    <!-- command 리스트 조회 -->
    <select id="getShallList" resultType="CommandDTO">
        SELECT   id, shall, created_date
        FROM     tb_command
        ORDER BY created_date desc
    </select>

    <!-- shall 명령어 등록 -->
    <insert id="addShall" parameterType="CommandDTO">
        INSERT INTO tb_command (shall)
        VALUES (#{shall})
        ON DUPLICATE KEY UPDATE created_date = CURRENT_TIMESTAMP
    </insert>
</mapper>