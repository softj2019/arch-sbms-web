<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.archivsoft.sbms.mapper.ControlMapper">
    <!-- 통합 제어 로그 삽입 -->
    <insert id="addLog" parameterType="ControlDTO">
        INSERT INTO tb_control_log (
              terminal_id
            , device_name
            , action
            , user_id
        ) VALUES (
              #{terminalId}
            , #{deviceName}
            , #{action}
            , #{userId}
        )
    </insert>
</mapper>